version: '3'
services:
  nginx:
    image: nginx:alpine
    ports:
      - '80:80'
    volumes:
      - './conf/nginx.conf:/etc/nginx/conf.d/default.conf'
      - './base.html:/srv/brandslist/base.html'
    links:
      - 'uwsgi'
  uwsgi:
    build:
      context: .
    depends_on:
      - 'postgres'
    volumes:
      - './:/srv/brandslist'
  postgres:
    image: postgres:9-alpine
    restart: always
    environment:
      POSTGRES_USER: 'brands'
      POSTGRES_PASSWORD: 'vGa4LA1u33'
    volumes:
      - "brandsdb:/var/lib/postgresql/data"
volumes:
  brandsdb: